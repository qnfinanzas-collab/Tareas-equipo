// Placeholder hasta el commit #4. Mantiene la estructura para que el tab
// no rompa al cambiar antes de que el dashboard esté implementado.
import React from "react";
export default function FinanceDashboard() {
  return (
    <div style={{ padding: 30, textAlign: "center", color: "#9CA3AF", fontSize: 13, fontStyle: "italic" }}>
      Dashboard financiero — implementado en el siguiente commit.
    </div>
  );
}
